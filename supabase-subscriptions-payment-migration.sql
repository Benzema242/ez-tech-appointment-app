-- DEPRECATED -- folded into supabase-subscriptions-setup.sql
-- payment_method column is already defined in the subscriptions setup file.
-- Do NOT run this separately.

-- alter table subscriptions
--   add column if not exists payment_method text not null default 'Bank Transfer';
